import styled from 'styled-components';

export const InputText = styled.input`
  border-radius: 4px;
  box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.25);
  padding: 2px 4px;
  outline: none;
  border: none;
`;

export const SelectedQuantity = styled.p`
  text-align: center;
  font-family: Roboto;
  font-size: 13px;
  color: #fff;
`;
